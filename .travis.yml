language: java
jdk:
  - oraclejdk8
before_script: 
  - cd jacobi
  - $GPG_EXECUTABLE --cipher-algo AES256
  - if [ ! -z "$GPG_SECRET_KEYS" ]; then echo $GPG_SECRET_KEYS | base64 --decode | $GPG_EXECUTABLE --import; fi
script:
  mvn test
cache:
  directories:
    - ~/.m2/repository