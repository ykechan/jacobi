language: java
jdk:
  - oraclejdk8
before_script: 
  - cd jacobi
  - if [ ! -z "$GPG_SECRET_KEYS" ]; then echo $GPG_SECRET_KEYS | $GPG_EXECUTABLE --import; fi
  - if [ ! -z "$GPG_OWNERTRUST" ]; then echo $GPG_OWNERTRUST | $GPG_EXECUTABLE --import-ownertrust; fi
script:
  mvn test
cache:
  directories:
    - ~/.m2/repository